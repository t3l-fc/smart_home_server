#include "SwitchManager.h"
#include <Arduino.h>

// Empty implementation - all methods are defined in the header with inline implementations
// This file exists only to satisfy the compiler's expectation that there is a .cpp file
// for each .h file in the project. 